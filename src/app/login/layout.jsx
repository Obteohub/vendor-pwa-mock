// Login page layout with optimized metadata
export const metadata = {
  title: 'Login - Shopwice Vendor',
  description: 'Sign in to your vendor dashboard',
};

export default function LoginLayout({ children }) {
  return children;
}
